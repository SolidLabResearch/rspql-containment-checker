superquery:
PREFIX ex: <http://example.org/>

SELECT *
WHERE {
  ?s a ex:Person .
  ?s ex:hasAge ?age .
  FILTER(?age > 10)
}

subquery : 
PREFIX ex: <http://example.org/>

SELECT ?s
WHERE {
  ?s a ex:Person .
  ?s ex:hasAge ?age .
  FILTER(?age > 30)
}